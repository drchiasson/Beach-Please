# Installed on the server via `crontab crontab.txt` by the cron-trigger deploy key.
# Runs once daily at 07:00 server-local time. Edit the schedule here and redeploy to change it.
PATH=/usr/local/bin:/usr/bin:/bin

#Run job at 10 am UTC (hence the 17) daily
0 17 * * * /opt/beach-please/app/run.sh >> /opt/beach-please/app/run.log 2>&1
